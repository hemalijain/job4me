$('#calendar').fullCalendar('removeEvents', [<%= @interview.id %>]);
$('#calendar').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%=j render(@interview, format: :json).html_safe %>"), 
  true
);
$('.modal').modal('hide');